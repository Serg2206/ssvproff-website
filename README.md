# SSVproff — Сайт научных публикаций

Добро пожаловать в репозиторий сайта **SSVproff** — персонального проекта Сергея В. Сушкова, посвященного хирургии, научным исследованиям, искусственному интеллекту и цифровым технологиям в медицине.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Структура проекта](#структура-проекта)
- [Требования](#требования)
- [Локальная разработка](#локальная-разработка)
- [Добавление новых публикаций](#добавление-новых-публикаций)
- [Развертывание на Netlify](#развертывание-на-netlify)
- [Настройка домена ssvnauka.net](#настройка-домена-ssvnaukanet)
- [Дополнительные настройки](#дополнительные-настройки)

---

## 📖 Описание проекта

Сайт **SSVproff** построен на генераторе статических сайтов [Hugo](https://gohugo.io/) с использованием темы PaperMod. Основные возможности:

- **Двуязычная поддержка**: Русский и английский языки
- **Научные публикации**: Структурированный каталог с метаданными
- **Поиск**: Полнотекстовый поиск по публикациям с использованием Lunr.js
- **SEO-оптимизация**: Мета-теги Open Graph и структурированные данные Schema.org
- **Готовность к развертыванию**: Настроенная конфигурация для Netlify

---

## 📁 Структура проекта

```
ssvproff-site/
├── config.toml                 # Основная конфигурация Hugo
├── netlify.toml                # Конфигурация для Netlify
├── README.md                   # Документация (этот файл)
│
├── content/                    # Контент сайта
│   ├── ru/                     # Русскоязычный контент
│   │   ├── _index.md           # Главная страница (RU)
│   │   ├── search.md           # Страница поиска (RU)
│   │   └── publications/       # Публикации на русском
│   │       └── холецистит.md
│   └── en/                     # Англоязычный контент
│       ├── _index.md           # Главная страница (EN)
│       ├── search.md           # Страница поиска (EN)
│       └── publications/       # Публикации на английском
│           └── cholecystitis.md
│
├── layouts/                    # Шаблоны Hugo
│   ├── _default/
│   │   ├── list.json.json      # Генератор JSON для поиска
│   │   ├── search.html         # Шаблон страницы поиска
│   │   └── single.html         # Шаблон отдельной публикации
│   └── partials/
│       ├── head.html           # Мета-теги и Schema.org
│       └── lang-switcher.html  # Переключатель языков
│
└── static/                     # Статические файлы
    ├── images/
    │   ├── logo.svg            # Логотип сайта
    │   └── favicons/           # Иконки для браузера
    ├── scripts/                # JavaScript файлы
    └── data/                   # Дополнительные данные
```

---

## ⚙️ Требования

### Для локальной разработки:
- **Hugo Extended** версии 0.128.0 или выше
- **Git** для контроля версий
- Текстовый редактор (VS Code, Sublime Text и т.д.)

### Для развертывания:
- Аккаунт на [Netlify](https://www.netlify.com/)
- Аккаунт на [GitHub](https://github.com/) (опционально, но рекомендуется)

---

## 🚀 Локальная разработка

### 1. Установка Hugo

#### macOS (через Homebrew):
```bash
brew install hugo
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt-get install hugo
```

#### Windows (через Chocolatey):
```bash
choco install hugo-extended
```

Или скачайте бинарный файл с [официального сайта Hugo](https://gohugo.io/installation/).

### 2. Проверка установки
```bash
hugo version
```

Должно вывести что-то вроде: `hugo v0.128.0+extended`

### 3. Запуск локального сервера

Перейдите в директорию проекта:
```bash
cd ssvproff-site
```

Запустите локальный сервер Hugo:
```bash
hugo server -D
```

Параметры:
- `-D` — показывать черновики (draft: true)
- `--navigateToChanged` — автоматически переходить к измененным страницам
- `--disableFastRender` — отключить быстрый рендеринг (для отладки)

Сайт будет доступен по адресу: [http://localhost:1313](http://localhost:1313)

### 4. Сборка для продакшена
```bash
hugo --gc --minify
```

Сгенерированные файлы будут в директории `public/`.

---

## ✍️ Добавление новых публикаций

### Шаг 1: Создайте файл публикации

Для **русской** публикации:
```bash
content/ru/publications/название-публикации.md
```

Для **английской** публикации:
```bash
content/en/publications/publication-title.md
```

### Шаг 2: Добавьте frontmatter (метаданные)

Пример для русской публикации:
```markdown
---
title: "Заголовок публикации"
authors:
  - name: "С. В. Сушков"
    orcid: "0000-0000-0000-0000"
  - name: "Второй Автор"
    orcid: "0000-0000-0000-0001"
year: 2024
journal: "Название журнала"
doi: "10.1000/xxxx"
pdf: "/static/pdfs/название-файла.pdf"
lang: "ru"
tags: ["хирургия", "холецистит", "ИИ"]
featured: true
bibtex: "/static/bib/название-файла.bib"
description: "Краткое описание публикации"
---

## Введение

Текст публикации...
```

### Шаг 3: Описание полей frontmatter

| Поле | Обязательно | Описание |
|------|-------------|----------|
| `title` | ✅ | Заголовок публикации |
| `authors` | ✅ | Список авторов (массив объектов) |
| `authors.name` | ✅ | Имя автора |
| `authors.orcid` | ❌ | ORCID автора |
| `year` | ✅ | Год публикации |
| `journal` | ❌ | Название журнала |
| `doi` | ❌ | DOI публикации |
| `pdf` | ❌ | Ссылка на PDF файл |
| `lang` | ✅ | Язык публикации (ru/en) |
| `tags` | ✅ | Теги для поиска |
| `featured` | ❌ | Избранная публикация (true/false) |
| `bibtex` | ❌ | Ссылка на BibTeX файл |
| `description` | ❌ | Краткое описание для SEO |

### Шаг 4: Добавьте контент

После frontmatter пишите текст публикации в формате Markdown:

```markdown
## Заголовок раздела

Обычный текст публикации.

### Подраздел

- Список пункт 1
- Список пункт 2

**Жирный текст** и *курсив*.

[Ссылка на внешний ресурс](https://example.com)

![Изображение](/images/название.png)
```

### Шаг 5: Добавьте PDF и BibTeX (опционально)

Если у вас есть PDF и BibTeX файлы:

1. Положите PDF в `static/pdfs/`
2. Положите BibTeX в `static/bib/`
3. Укажите пути в frontmatter

---

## 🌐 Развертывание на Netlify

### Вариант 1: Развертывание через GitHub (рекомендуется)

#### Шаг 1: Создайте репозиторий на GitHub
1. Зайдите на [GitHub](https://github.com/) и создайте новый репозиторий
2. Назовите его, например, `ssvproff-website`
3. Инициализируйте Git в локальной директории:

```bash
cd ssvproff-site
git init
git add .
git commit -m "Initial commit: SSVproff website"
```

4. Подключите удаленный репозиторий:

```bash
git remote add origin https://github.com/ваш-username/ssvproff-website.git
git branch -M main
git push -u origin main
```

#### Шаг 2: Подключите репозиторий к Netlify
1. Зайдите на [Netlify](https://www.netlify.com/) и войдите в аккаунт
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **"Deploy with GitHub"**
4. Авторизуйте Netlify для доступа к вашим репозиториям
5. Выберите репозиторий `ssvproff-website`

#### Шаг 3: Настройте параметры сборки
Netlify автоматически обнаружит файл `netlify.toml` и использует его настройки:

- **Build command**: `hugo --gc --minify`
- **Publish directory**: `public`
- **Hugo version**: `0.128.0`

Нажмите **"Deploy site"**

#### Шаг 4: Дождитесь завершения сборки
Netlify автоматически соберет и развернет ваш сайт. Это займет 1-3 минуты.

После успешного развертывания вы получите временный URL вида:
```
https://random-name-123456.netlify.app
```

---

### Вариант 2: Развертывание через Drag & Drop

Если вы не хотите использовать GitHub:

#### Шаг 1: Соберите сайт локально
```bash
cd ssvproff-site
hugo --gc --minify
```

#### Шаг 2: Загрузите на Netlify
1. Зайдите на [Netlify](https://www.netlify.com/)
2. Перетащите папку `public/` в окно браузера на странице Netlify
3. Netlify автоматически развернет сайт

**Минусы этого подхода:**
- Нет автоматических обновлений при изменении контента
- Нет истории версий
- Нужно вручную пересобирать и загружать каждый раз

---

## ⚠️ Возможные проблемы при развертывании

### Ошибка: "module 'papermod' not found"

**Проблема**: При сборке на Netlify возникает ошибка, что тема PaperMod не найдена.

**Причина**: В файле `config.toml` была указана строка `theme = "papermod"`, но сама тема не включена в проект (нет папки `themes/papermod` или git submodule).

**Решение**: 
Эта проблема уже исправлена в текущей версии проекта. Строка `theme = "papermod"` закомментирована в `config.toml`, так как проект использует собственные layouts из папки `layouts/` и не требует внешней темы.

Если вы столкнулись с этой ошибкой, убедитесь, что в `config.toml` строка закомментирована:
```toml
# theme = "papermod"  # Закомментировано: проект использует собственные layouts
```

**Подробное объяснение**: См. файл `ИСПРАВЛЕНИЕ_ОШИБКИ.md` в корне проекта.

### Ошибка: "failed to resolve output format"

**Проблема**: Hugo не может разрешить выходной формат JSON.

**Решение**: 
Убедитесь, что в `config.toml` правильно настроены outputs:
```toml
[outputs]
  home = ["HTML", "JSON"]
```

И что существует файл `layouts/_default/list.json.json` для генерации JSON.

### Ошибка: "Error building site: failed to render pages"

**Проблема**: Ошибка при рендеринге страниц.

**Возможные причины и решения**:
1. **Неправильный синтаксис Markdown**: Проверьте все `.md` файлы на наличие синтаксических ошибок
2. **Отсутствующие frontmatter поля**: Убедитесь, что все обязательные поля заполнены
3. **Битые ссылки**: Проверьте все внутренние ссылки на существование файлов

### Ошибка: "command not found: hugo"

**Проблема**: Hugo не установлен или неправильно указана версия в `netlify.toml`.

**Решение**: 
Проверьте файл `netlify.toml`:
```toml
[build.environment]
  HUGO_VERSION = "0.128.0"
```

Убедитесь, что версия Hugo актуальна и поддерживается.

### Сайт собрался, но поиск не работает

**Проблема**: Страница поиска открывается, но поиск не выдает результатов.

**Решение**:
1. Проверьте, что генерируется файл `index.json`:
   - Откройте `https://ваш-сайт.netlify.app/index.json`
   - Должен отобразиться JSON с публикациями
2. Проверьте консоль браузера (F12) на наличие JavaScript ошибок
3. Убедитесь, что файл `static/scripts/search.js` существует и загружается

### Проблемы с многоязычностью

**Проблема**: Переключатель языков не работает или показывает 404.

**Решение**:
1. Убедитесь, что в `config.toml` правильно настроены языки:
```toml
[languages]
  [languages.ru]
    contentDir = "content/ru"
  [languages.en]
    contentDir = "content/en"
```
2. Проверьте, что существуют файлы `content/ru/_index.md` и `content/en/_index.md`

### Изображения не загружаются

**Проблема**: Изображения отображаются локально, но не на Netlify.

**Решение**:
1. Убедитесь, что изображения находятся в папке `static/`
2. Используйте абсолютные пути, начинающиеся с `/`:
   ```markdown
   ![Логотип](/images/logo.svg)
   ```
   Вместо относительных:
   ```markdown
   ![Логотип](../images/logo.svg)  ❌ Неправильно
   ```

### Дополнительная помощь

Если проблема не решается:
1. Проверьте логи сборки в Netlify Dashboard
2. Попробуйте собрать сайт локально: `hugo --gc --minify`
3. Проверьте, что все файлы закоммичены в Git: `git status`
4. Посмотрите документацию Hugo: [https://gohugo.io/documentation/](https://gohugo.io/documentation/)

---

## 🔗 Настройка домена ssvnauka.net

### Шаг 1: Откройте настройки домена в Netlify
1. Зайдите в настройки сайта на Netlify
2. Перейдите в **"Domain management"** → **"Add custom domain"**
3. Введите `ssvnauka.net`

### Шаг 2: Настройте DNS у регистратора домена

У вашего регистратора домена (например, GoDaddy, Namecheap, REG.RU) добавьте следующие DNS записи:

#### Вариант А: Использование Netlify DNS (рекомендуется)
1. В Netlify перейдите в **"Domain management"** → **"Netlify DNS"**
2. Добавьте домен `ssvnauka.net`
3. Netlify предоставит вам name-серверы, например:
   ```
   dns1.p01.nsone.net
   dns2.p01.nsone.net
   dns3.p01.nsone.net
   dns4.p01.nsone.net
   ```
4. У регистратора домена замените NS записи на предоставленные Netlify

#### Вариант Б: Использование A-записей
Если вы не хотите переносить DNS на Netlify, добавьте A-запись:

| Type | Name | Value |
|------|------|-------|
| A | @ | 75.2.60.5 |
| CNAME | www | ваш-сайт.netlify.app |

### Шаг 3: Включите HTTPS
1. В Netlify перейдите в **"Domain management"** → **"HTTPS"**
2. Нажмите **"Verify DNS configuration"**
3. После успешной верификации нажмите **"Provision certificate"**
4. Дождитесь получения SSL-сертификата (Let's Encrypt)

**Готово!** Ваш сайт будет доступен по адресу `https://ssvnauka.net` через несколько минут после обновления DNS (обычно 30 минут — 24 часа).

---

## 🛠️ Дополнительные настройки

### Настройка поисковой оптимизации (SEO)

#### 1. Обновите файл `config.toml`:
```toml
[params]
  author = "Сергей В. Сушков"
  description = "Хирургия, наука, ИИ и цифровые медиа"
  keywords = ["хирургия", "медицина", "ИИ", "научные публикации"]
  
  # Open Graph и Twitter Cards
  images = ["/images/logo.svg"]
  
  # Google Analytics
  googleAnalytics = "G-XXXXXXXXXX"  # Ваш GA4 ID
```

#### 2. Создайте файл `robots.txt` в `static/`:
```
User-agent: *
Allow: /

Sitemap: https://ssvnauka.net/sitemap.xml
```

#### 3. Добавьте Google Search Console:
1. Зайдите на [Google Search Console](https://search.google.com/search-console/)
2. Добавьте свой домен `ssvnauka.net`
3. Подтвердите владение через DNS или HTML-файл
4. Отправьте sitemap: `https://ssvnauka.net/sitemap.xml`

---

### Добавление Google Analytics

1. Получите ID отслеживания на [Google Analytics](https://analytics.google.com/)
2. Добавьте в `config.toml`:
```toml
googleAnalytics = "G-XXXXXXXXXX"
```
3. Hugo автоматически добавит код отслеживания

---

### Настройка переадресации (Redirects)

Если нужно настроить редиректы, отредактируйте `netlify.toml`:

```toml
[[redirects]]
  from = "/old-page"
  to = "/new-page"
  status = 301

[[redirects]]
  from = "/blog/*"
  to = "/publications/:splat"
  status = 301
```

---

## 🔄 Обновление контента

### Через GitHub (если используете Git):
1. Внесите изменения в локальные файлы
2. Закоммитьте и запушьте:
```bash
git add .
git commit -m "Добавлена новая публикация"
git push origin main
```
3. Netlify автоматически пересоберет сайт

### Через Netlify вручную:
1. Соберите сайт локально: `hugo --gc --minify`
2. Загрузите папку `public/` на Netlify

---

## 📞 Поддержка и контакты

- **Автор**: Сергей В. Сушков
- **Email**: [укажите email]
- **Сайт**: [https://ssvnauka.net](https://ssvnauka.net)

---

## 📄 Лицензия

© 2024 SSVproff. Все права защищены.

---

## 🎉 Заключение

Поздравляем! Теперь у вас есть полностью настроенный Hugo-сайт, готовый к развертыванию на Netlify. 

**Следующие шаги:**
1. ✅ Добавьте свои научные публикации
2. ✅ Настройте домен ssvnauka.net
3. ✅ Настройте Google Analytics
4. ✅ Отправьте sitemap в Google Search Console
5. ✅ Продвигайте свои исследования!

Удачи с вашим проектом! 🚀
